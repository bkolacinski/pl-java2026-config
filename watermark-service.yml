spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 25MB
server:
  port: 8082
watermark:
  app-key: ${WATERMARK_APP_KEY:local-dev-watermark-secret}
springdoc:
  swagger-ui:
    path: /swagger-ui.html
